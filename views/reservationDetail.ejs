<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>예약 세부 사항</title>
  <link rel="stylesheet" href="/stylesheets/css/bootstrap/bootstrap.min.css" crossorigin="anonymous">
  <script src="/javascripts/jquery/jquery.min.js" type="text/javascript"></script>
  <script src="/javascripts/reservation.js" defer></script>
</head>
<body>
    <header>
        <div class="navbar navbar-dark bg-dark shadow-sm mb-3">
            <div class="container d-flex justify-content">
                <a href="/" class="navbar-brand d-flex align-items-center">
                    <strong>도서관 예약 현황</strong>
                </a>
            </div>
        </div>
    </header>
  <table class = "table">
    <thead class="thead-light">
      <tr class = "text-center">
        <th>좌석번호</th>
        <th>잔여 시간</th>
        <th>예약되었는지?</th>
        <th>예약</th>
      </tr>
    </thead>
    <tbody>
      <tr class="text-center">
        <td><%= seat.seatNumber %></td>
        <td><%= seat.remainingTime %> 분</td>
        <td><%= seat.isReserved ? '예약 중' : '예약 가능' %></td>
        <td>
          <% if (!seat.isReserved) { %>
            <button class="board_button btn btn-primary btn-sm rounded-pill" id="reserveButton" data-seat-number="<%= seat.seatNumber %>">예약</button>
          <% } else { %>
            <button class="board_button btn btn-secondary btn-sm rounded-pill" id="cancelButton" data-seat-number="<%= seat.seatNumber %>">예약 취소</button>
          <% } %>
        </td>
      </tr>
    </tbody>
  </table>
<h1></h1>
</body>
</html>
